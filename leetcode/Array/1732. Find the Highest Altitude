class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int sum = 0;
        int tp = 0;
        for(auto x:gain)
        {
            tp+=x;
             sum = max(sum,tp);
        }
        return sum;
        }
};
