class Solution {
public:
    int busyStudent(vector<int>& s, vector<int>& e, int t) {
        int c = 0;
        for(int i=0;i<s.size();i++){
            if(t>=s[i] && t<=e[i])
                c++;
            }
        return c;
    }
};
