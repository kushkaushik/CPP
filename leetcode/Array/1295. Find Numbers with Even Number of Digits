class Solution {
public:
    int findNumbers(vector<int>& nums) {
int c =0;
        for(int i =0;i<nums.size();i++)
        {
             int tp = floor(log10(nums[i])+1);
    if(tp%2 != 1)
    c++;
                
        }
        return c;
        
        }
};
